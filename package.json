{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "Monorepo for backend and frontend applications",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.2.2"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "uuid": "^11.1.0"
  }
}
